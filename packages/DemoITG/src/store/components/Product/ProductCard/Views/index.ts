import { CartMode, DefaultMode, SimpleMode, SlimMode,WishlistMode } from "./Modes"

const modes = {
    default: DefaultMode,
    simple: SimpleMode,
    slim: SlimMode,
    cart: CartMode,
    wishlist: WishlistMode
}

export default modes